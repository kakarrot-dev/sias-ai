# 2026-09-17 Web 原型导入验证

本次在独立目录安装依赖并验证前后台，不修改来源工作区。导入包括来源工作区未提交的最新原型代码；逐文件 SHA-256 见 `import-manifest.json`。

| 检查 | 结果 |
| --- | --- |
| 前后台分别 `npm ci` | 通过，未复用来源工作区 node_modules |
| 前台 TypeScript、排版契约、布局契约与生产构建 | 通过 |
| 前台 `npm test` | 17 个文件、183 项通过 |
| 后台 TypeScript 与生产构建 | 通过 |
| 后台 `npm test` | 27 个文件、445 项通过 |
| 269 个原始源码、资源、配置和文档文件 SHA-256 | 与来源快照一致 |
| 提交范围与常见凭证格式检查 | 无凭证、用户数据库、依赖或构建产物纳入提交 |
| 浏览器生产预览 | 前台新对话与 32 位专家目录正常；后台智能体列表与创建专家页正常；两端未观察到控制台 error |

初次后台测试发现动态 glob 读取的 Skill 说明缺失，已补齐原始 `src/runtime/skill-packages/*/SKILL.md` 静态资源，再次完整测试 445 项通过。没有修改业务代码或放宽测试断言。

已知提示：两端构建均有大于 500 kB 的 chunk 提示；安装时可选 fsevents 脚本未授权，未影响本次构建与测试。前台测试保留 Node localStorage 实验性提示。

`git diff --cached --check` 报告 3 处来源文件空白：后台 `InputOutputWorkspace.tsx` 的文件末尾空行，以及前台 PRD 第 3、4 行的 Markdown 双空格换行。本次按原始文件保留，未为迁移重写源码或文档格式。新增仓库配置与启动文档没有额外空白问题。

浏览器检查仅证明迁移后的静态产物可以加载和打开关键入口，不代表真实模型、OIDC、工具执行或前后台数据贯通验收。本次未部署在线站点。
